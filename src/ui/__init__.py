"""Streamlit UI layer for the position manager / screener.

Each module owns one section of the page (sidebar, positions, recommendations,
charts, daily report). ``src/app.py`` wires them together into a single page.
"""
